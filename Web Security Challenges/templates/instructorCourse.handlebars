<!DOCTYPE html>
<html>
<head>
    <style>
        table {
            border: 2px solid black;
            border-collapse: collapse;
            width: 80%;
            margin: 0 auto;
        }
        th, td {
            border: 2px solid black;
            padding: 8px;
            text-align: center;
        }
        th {
            background-color: #f2f2f2;
        }
        h1 {
            font-size: 24px;
        }
    </style>
</head>
<body>
    <h1>All Courses</h1>
    <table>
        <thead>
            <tr>
                <th>Code</th>
                <th>Name</th>
                <th>Current Capacity</th>
            </tr>
        </thead>
        <tbody>
            {{#each courses}}
                <tr>
                    <td><a href="/instructor/course/{{this.code}}">{{this.code}}</a></td>
                    <td>{{this.name}}</td>
                    <td>{{this.capacity}}</td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html>